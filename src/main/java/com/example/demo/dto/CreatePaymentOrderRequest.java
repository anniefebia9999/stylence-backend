package com.example.demo.dto;

public class CreatePaymentOrderRequest {
    // No fields needed right now — amount is calculated server-side from the user's actual cart,
    // never trusted from the frontend (prevents someone tampering with the price).
}